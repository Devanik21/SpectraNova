"""Utility functions for data loading and preprocessing."""

from spectranova.core.types import SignalType

def normalize_signal(signal: SignalType) -> SignalType:
    """Normalize signal to [-1, 1]."""
    pass
